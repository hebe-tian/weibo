name: rank
on:
  schedule:
    - cron: '30 * * * *'
jobs:
  work:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: '3.7'
      - run: pip install -r requirement.txt
      - run: run python .\bin.py
